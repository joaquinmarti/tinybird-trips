import ChartElement from "../../../src/widgets/chart/chart-element";
import "../../../src/widgets/chart";

describe("Widgets / Chart / Chart element", () => {
  test("should be defined", () => {
    expect(ChartElement).toBeDefined();
  });

  /* Expand tests on web component */
});